import 'package:atlas_app/imports.dart';

class DashPage extends ConsumerWidget {
  const DashPage({super.key, required this.dashId});

  final String dashId;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold();
  }
}
